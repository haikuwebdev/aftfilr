<h1>Editing <%= singular_name %></h1>

<%% form_for(@<%= singular_name %>) do |f| %>
  <%%= f.error_messages %>
  <%%= f.select :<%= category_singular_name %>_id, <%= category_model_class_name %>.find(:all).collect{|cat| [cat.name, cat.id]} %>
  <p><%%= f.submit "Update" %></p>
<%% end %>

<%%= link_to 'Back', <%= plural_name %>_path %>
